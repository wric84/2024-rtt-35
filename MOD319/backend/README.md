# Backend API template

## Instructions

1. Clone the repo 'git close <repo-url> <new-project-name>'
2. cd into your new project folder and run npm i
3. create a new `.env` file and add the `MONGODB_URI`
Run the app with: `npm run dev`