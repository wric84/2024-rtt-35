//On the console log the result, is five greater than four.
//On the console log the result, is five less than four.

//On the console log the result, is three greater than or equal to than four.

//On the console log the result, is three the same as the three

//On the console log the result, is the number three the same as the string three
let variableA = 5;
let variableB = 4;
let variableC = a + b;

console.log(`The answer to ${variableA} + ${variableB} is ${variableC}`);

console.log(
  "The answer to " + variableA + " + " + variableB + " is " + variableC
);

let key = "second";
switch (key) {
  case "first":
    console.log("Gold Medal");

    break;

  case "second":
    console.log("Silver Medal");

    break;
  case "third":
    console.log("bronze Medal");

    break;

  default:
    break;
}
